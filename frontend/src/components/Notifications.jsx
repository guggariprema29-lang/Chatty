// Notifications UI was removed per user request. Keep a harmless placeholder
// to avoid import errors in places where the component was previously used.
const Notifications = () => null;
export default Notifications;
